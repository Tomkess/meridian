# Vision

Meridian is the development OS for AI-native teams: capture every idea, shape it into a spec,
plan the work, build with confidence, and ship — all within a single, file-backed workflow
that any AI agent can read and act on without losing context across sessions.
